import React from 'react';
import ReactDOM from 'react-dom/client';
import SkillRackDashboard from './SkillRackDashboard';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SkillRackDashboard />
  </React.StrictMode>
);